defmodule TagIp.Repo.Migrations.CreateOrganizationEventDefinitions do
  use Ecto.Migration

  def up do
    # Rien à faire, les colonnes et index sont déjà alignés en base de données
  end

  def down do
    # Optionnel
  end
end
